const ROLES = {
	ROOT: 'root',
	OWNER: 'owner',
	ADMIN: 'admin',
	MEMBER: 'member',
};

const LIMIT = 100;

const BATCH_SIZE = 50;

const LIMIT_SEARCH_RESULTS = 20;

module.exports = {
	ROLES,
	LIMIT,
	BATCH_SIZE,
	LIMIT_SEARCH_RESULTS,
};
